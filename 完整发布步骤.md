# 📦 VSCode 插件发布步骤

本指南专注于插件的发布流程，开发相关内容请参考 [README.md](README.md)。

---

## 📋 第一步：准备工作

### 1.1 检查 package.json 配置

确保 `package.json` 中的以下字段已正确配置：

- ✅ `publisher`: 必须是你在 Marketplace 中创建的发布者名称（例如：`"liyunhua"`）
- ✅ `version`: 版本号（例如：`"0.1.0"`）
- ✅ `name`: 插件名称（例如：`"algorithm-template"`）

**重要**：`publisher` 必须与 Marketplace 中的发布者名称完全一致！

### 1.2 编译代码

```bash
npm run compile
```

确保编译成功，生成 `out/extension.js` 文件。

---

## 📋 第二步：创建 Personal Access Token

### 2.1 访问 Azure DevOps

1. 访问：https://dev.azure.com
2. 使用你的 Microsoft 账号登录

### 2.2 创建 Token

1. 点击右上角的用户头像
2. 选择 "Security" 或 "Personal access tokens"
3. 点击 "+ New Token" 或 "New Token" 按钮
4. 填写 Token 信息：
   - **Name**: `VSCode Extension Publishing`
   - **Organization**: 选择你的组织
   - **Expiration**: 选择 `90 days` 或 `1 year`
5. **⚠️ 关键：设置权限（Scopes）**：
   - 展开 **"Custom defined"**
   - 找到 **"Marketplace"**
   - ✅ **勾选 "Marketplace"**
   - 展开 "Marketplace" 选项
   - ✅ **勾选 "Manage"**（这是发布插件必需的权限！）
6. 点击 "Create" 按钮
7. **⚠️ 立即复制 Token**：Token 只会显示一次！保存到安全的地方。

---

## 📋 第三步：登录到 VSCode 市场

### 3.1 安装 vsce 工具（如果还没安装）

```bash
npm install -g @vscode/vsce
```

### 3.2 使用 Token 登录

```bash
vsce login liyunhua
```

**注意**：将 `liyunhua` 替换为你在 `package.json` 中设置的 `publisher` 名称。

### 3.3 输入 Token

- 系统会提示：`Personal Access Token for publisher 'liyunhua':`
- 粘贴你刚才复制的 Token
- 按回车确认

### 3.4 验证登录

```bash
vsce ls-publishers
```

如果能看到你的发布者列表，说明登录成功！

---

## 📋 第四步：发布插件

### 4.1 发布插件

```bash
vsce publish
```

### 4.2 等待发布完成

- 发布过程可能需要几分钟
- 如果成功，会显示类似消息：
  ```
  Published liyunhua.algorithm-template v0.1.0
  ```

### 4.3 访问插件页面

发布成功后，你的插件会在以下地址可见：
```
https://marketplace.visualstudio.com/items?itemName=liyunhua.algorithm-template
```

**注意**：可能需要等待几分钟才能在市场中搜索到。

---

## 🔄 更新插件版本

当你需要更新插件时：

1. **更新版本号**：在 `package.json` 中修改 `version` 字段
2. **重新编译**：`npm run compile`
3. **重新发布**：`vsce publish`

---

## ❓ 常见问题

### Q1: 发布时出现 401 错误

**原因**：Token 无效或缺少权限

**解决方法**：
1. 检查 Token 是否过期
2. 确保 Token 有 "Marketplace - Manage" 权限
3. 重新创建 Token 并登录：`vsce login liyunhua`

### Q2: 发布时提示 publisher 不存在

**解决方法**：
1. 访问：https://marketplace.visualstudio.com/manage
2. 创建发布者，Publisher ID 必须与 `package.json` 中的 `publisher` 完全一致

### Q3: 打包时包含不需要的文件

**解决方法**：
- 检查 `.vscodeignore` 文件
- 确保不需要的文件已添加到忽略列表

---

## ✅ 发布检查清单

在发布前，确保：

- [ ] `package.json` 中的 `publisher` 与 Marketplace 中的发布者名称一致
- [ ] `package.json` 中的 `version` 已更新
- [ ] 代码已编译（`npm run compile` 成功）
- [ ] Personal Access Token 已创建，且有 "Marketplace - Manage" 权限
- [ ] 已使用 Token 登录（`vsce login` 成功）
- [ ] 发布成功（`vsce publish` 无错误）

---

## 📚 相关资源

- VSCode 扩展市场：https://marketplace.visualstudio.com
- vsce 工具文档：https://github.com/microsoft/vscode-vsce
- Azure DevOps：https://dev.azure.com
